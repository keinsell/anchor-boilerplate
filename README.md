# Anchor Boilerplate

### Intended Usage

Whole process should be managed by `config` directory.

```
$ cp package <desired_location>
$ cd <desired_location>
$ pnpm install
$ pnpm run solana:setup
$ pnpm run build
$ pnpm run test
$ pnpm run deploy
```
